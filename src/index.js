import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

ReactDOM.render(
  // React.StrictMode removed as Material UI currently does not support it.
  <App />,
  document.getElementById('root')
)
